import AddBalance from "./AddBalance";

const SettingContent = ({children}) => {
  return (
    <div className="bg-white shadow-lg h-full w-full p-4">
      {children}
    </div>
  );
};

export default SettingContent;
